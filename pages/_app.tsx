import '../resources/css/globals-light.css'

import { ChallengesProvider } from '../resources/context/ChallengesContext'

function MyApp({ Component, pageProps }) {

    return (
        <Component {...pageProps} />
    ) 
}

export default MyApp
